// Used when creating your project as executable
// Can use package blahblah to create "reusable" code, i.e. library
package main

// All native libraries at golang.org/pkg
import "fmt"

// func == function (same as python def)
func main() {
	fmt.Println("Hi there!")
}
